--recommended structure
	my_project/
	├── src/
	│   ├── core/       # Core game/data structures
	│   ├── render/     # Graphics-related code
	│   └── utils/      # Helper functions
	├── assets/         # Images, sounds
	├── build/          # Compiler output (add to .gitignore)
	└── main.odin       # Entry point, imports from src/


--breakout tutorial: https://github.com/karl-zylinski/breakout/tree/main

--importing glb:
*   RAYLIB LIMITATIONS:
*     - Only supports 1 armature per file, and skips loading it if there are multiple armatures
*     - Only supports linear interpolation (default method in Blender when checked
*       "Always Sample Animations" when exporting a GLTF file)
*     - Only supports translation/rotation/scale animation channel.path,
*       weights not considered (i.e. morph targets)
	
	To export transparent textures on a glb model:
	Use Principled BSDF: Connect the alpha output of an image texture to the alpha input 
	of the Principled BSDF shader.

--blener fast export glb: F4->e->l




PRR S2 6-8-6 ST locomotive
 — 
Вчера, в 23:22
hello. i import models from blender, glb files. i want to be able to use custom properties. like for example blender object Plain Axis with custom property actor_pos as boolean. i want to set my actor position to this object position on start if it has property actor_pos.
NitroPlum
 — 
Вчера, в 23:33
Gltf file format includes the custom props
Znarf
 — 
Вчера, в 23:34
you have to tick the option in the blender gltf exporter and then it will be in the "extras" field of the node
PRR S2 6-8-6 ST locomotive
 — 
Вчера, в 23:34
so i have to parse it twice? oh well
i mean once raylib does it for me and once i do it manually
ok thank you (:
Znarf
 — 
Вчера, в 23:37
you can use vendor:cgltf, everything node (and accessor, sample etc) has an extras field, you will have to parse the json yourself tho 
NitroPlum
 — 
Вчера, в 23:38
U can #load the file and pass the memory to cgltf 
